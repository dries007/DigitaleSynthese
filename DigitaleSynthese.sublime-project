{
	"folders":
	[
		{
			"path": "."
		}
	],
    "build_systems":
    [
        {
            "name": "GHDL",
            "working_dir": "${project_path}",
            "shell_cmd": "mkdir -p simu; shopt -s globstar; ghdl -i --workdir=simu --std=93c --ieee=synopsys -fexplicit src/**/*.vhd; ghdl -m --workdir=simu --std=93c --ieee=synopsys -fexplicit $file_base_name; ghdl -r $file_base_name --vcd=simu/wave.vcd"
        }
    ]
}
